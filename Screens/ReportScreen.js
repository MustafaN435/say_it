import React from 'react';
import { View, Text } from 'react-native';

export default function ReportScreen() {
  return (
    <View><Text>Report a Crime Screen (Form UI later)</Text></View>
  );
}
// This is the ReportScreen component for the Say It app, which will eventually include a form for reporting crimes.
// It currently displays a simple text indicating that this is the report a crime screen.